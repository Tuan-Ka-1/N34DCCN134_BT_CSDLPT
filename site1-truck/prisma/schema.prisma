generator client {
  provider = "prisma-client-js"
}
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
model Truck {
  vehicle_id         String @id
  load_capacity_tons Float
}