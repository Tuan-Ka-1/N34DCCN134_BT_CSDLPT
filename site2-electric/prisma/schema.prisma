generator client {
  provider = "prisma-client-js"
}
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
model ElectricCar {
  vehicle_id           String @id
  battery_capacity_kwh Float
}